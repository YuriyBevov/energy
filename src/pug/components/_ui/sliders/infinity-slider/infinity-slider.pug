mixin infinity-slider(slides)
  .swiper.infinity-slider
    .swiper-wrapper
      each slide in slides
        .swiper-slide
          img(src=`./assets/img/${slide.img}.png` alt=slide.title width="200" height="120")
